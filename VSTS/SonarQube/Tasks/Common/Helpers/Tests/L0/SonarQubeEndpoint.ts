import * as assert from "assert";

describe("SonarQubeEndpoint", () => {
    describe("#getTaskSonarQubeEndpoint()", () => {
        it("should throw if the endpoint isn't defined", () => {
            assert.fail();
        });

        it("should throw if the connectedServiceName input isn't set", () => {
            assert.fail();
        });

        it("should throw if the URL of the endpoint isn't set", () => {
            assert.fail();
        });

        it("should return a new instance with the expected values", () => {
            assert.fail();
        });
    });
});